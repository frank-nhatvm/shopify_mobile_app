query LaunchList($pageSize: Int) {
    launches(pageSize: $pageSize) {
        launches {
            id
            mission {
                name
            }
            rocket {
                name
            }
            isBooked
        }
    }
}